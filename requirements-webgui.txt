# Web GUI + voice chat dependencies
fastapi
uvicorn
python-multipart           # file uploads in FastAPI
websockets
edge-tts                   # TTS (free, no API key)
miniaudio                  # MP3 → PCM decode (TTS pipeline)
faster-whisper             # STT for voice input (preferred)
# tiktoken                 # better tokenizer (optional, recommended)
# pyngrok                  # for --share public tunnel (optional)
